# URL gốc của backend API.
# - Chạy local (npm run dev): trỏ tới Spring backend đang chạy ở cổng 8080
# - Chạy bằng Docker (nginx proxy): để là /api (đã cấu hình sẵn trong Dockerfile)
VITE_API_URL="http://localhost:8080/api"
